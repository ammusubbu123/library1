def Sum(a,b)
{
   echo "The Sum of ${a} and ${b} is ${a+b}"
}
def Mul(a,b)
{
   echo "The prodeuct of ${a} and ${b} is ${a*b}"
   
}
def Division(p,q)
{
   echo " The division of ${p} and ${q} is ${p/q}"
   
 }
